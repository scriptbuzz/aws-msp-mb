.terraform/
*.tfstate
*.tfstate.backup
*.tfstate.lock.info
.terraform.lock.hcl
*.tfvars
!*.tfvars.example
backend.hcl
.env
.env.*
!.env.example
.DS_Store

# secrets / credentials — never commit
*.pem
*.key
*.p12
*.pfx
id_rsa
id_dsa
**/credentials
**/aws_credentials

# local-only reference material — not part of the project
references/

# local editor/tooling config
.claude/
aws-msp-ops06-port.zip
